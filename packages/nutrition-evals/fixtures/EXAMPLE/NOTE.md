# EXAMPLE fixture (template — not run)

This directory is a **template**, not a real eval case. It has an `expected.json` but
no image, so the harness skips it with a warning and it does not affect the report.

To create a real fixture, copy this directory, rename it, drop in a label photo, and edit
`expected.json` to match the label. See the package
[`README.md`](../../README.md) for the full step-by-step runbook.
