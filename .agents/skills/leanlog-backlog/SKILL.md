---
description: |
    Lists the leanlog backlog in GH issues.
    Use this skill in the following scenarios:
        - "list items from the backlog"
        - "what tasks are open"
        - **ANY TIME** the user asks to list, or explore tasks
        - For listing open tasks
argument-hint: "[Filtering]"
---



# TASK

List open backlog issues, optionally filtering per user requirements.

Open issues:

`!gh issue list --state open --limit 50`


If the user wants to see a specific query or perform a specific action relating to issue listing, follow the directions below:

```
$ARGUMENTS
```
